# Runtime dependencies, pinned to the versions this project is developed against.
# Test-only packages live in requirements-dev.txt.
# pyproject.toml holds the looser ranges used when installing as a package.

eval-type-backport==0.4.0; python_version < "3.10"
fastapi==0.128.8
httpx==0.28.1
openai==2.45.0
pydantic==2.13.4
pydantic-settings==2.11.0
uvicorn==0.39.0
